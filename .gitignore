frontend_src/node_modules/
frontend_src/dist/
backend/node_modules/
backend/dist/
backend/.env
backend/.env.*
*.log
.env
.env.production
piper-voices/
frontend/dist/
frontend/assets/
*.bak
backup_pre_fix_*.sql

# Ajouts 2026-05-08 - réconciliation backend serveur
*.bak2
*.bak3
*.bak.*
backend/node_modules_docker/
.github/SECRETS.md
.github/workflows/ci-cd.yml
uploads/
node_modules/
.DS_Store

# Dumps base de donnees (donnees personnelles, ne jamais versionner)
backup_*.sql
julaba_db_*.sql
*_backup.sql
*_dump.sql
erreur.txt

# Outils locaux (config/état de session, pas les skills partagées de l'équipe)
.claude/*
!.claude/skills/
.idea/

# Build frontend versionne par erreur (la source est dans frontend_src/, build via frontend_src/Dockerfile)
frontend/
frontend_src/tsconfig.tsbuildinfo

# Fichier intermediaire du banc de capture (apercu-caisse/capture.mjs, jetable)
frontend_src/apercu-caisse/.viewport-2x.png

# Journal d'execution des invariants (ci/prouver-invariants.mjs).
# Jetable par construction : il porte le SHA de l'arbre sur lequel il a tourne,
# donc le commiter l'invaliderait lui-meme. Il se reproduit a chaque passe.
ci/preuve-invariants.json
