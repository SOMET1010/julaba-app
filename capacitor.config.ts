import type { CapacitorConfig } from "@capacitor/cli"

const config: CapacitorConfig = {
    appId: "ci.julaba.app",
    appName: "julaba-app",
    webDir: "webDir=./frontend/dist",
}

export default config
