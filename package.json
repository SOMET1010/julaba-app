{
    "name": "julaba-app",
    "private": true,
    "version": "1.0.0",
    "workspaces": [
        "frontend",
        "backend",
        "tests"
    ],
    "scripts": {
        "dev": "npm run dev --workspace=frontend",
        "dev:backend": "npm run build --workspace=backend",
        "build": "npm run build --workspaces",
        "test": "npm run test --workspace=tests",
        "typecheck": "npm run typecheck --workspace=frontend"
    }
}
