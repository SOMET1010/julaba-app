/**
 * SimulateurBO — supprimé en production.
 * Ce composant ne rend rien. Il est conservé pour compatibilité d'import.
 */
import React from 'react';

interface SimulateurBOProps {
  isOpen?: boolean;
  onClose?: () => void;
  identificateurId?: string;
}

export function SimulateurBO(_props: SimulateurBOProps) {
  return null;
}
